An input for selecting or adding several values, such as tags. Pass a list of `items`, or a `dynamicItems` function for async suggestions, and handle new entries with `onitemadded`.

An item's `hint` is secondary text at the end of its row in the menu, for telling two of the same label apart. A staged chip reads the label alone.
