A text field styled for search, meant for toolbars and filter bars.

By default the field is minimal while empty: no backdrop, no search button and no
inline start padding. Set `filledBackdrop` to keep the filled appearance at all
times, e.g. when the field should read as a permanent control.
