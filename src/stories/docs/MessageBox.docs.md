An inline message block with an optional icon and a `neutral` or `warning` tone. It shows a close button when `dismissable` is set and an `onclose` handler is provided.

You can set `--tint--message-box-aside-bg` if you want to customize the background color of the aside section. The default is transparent
